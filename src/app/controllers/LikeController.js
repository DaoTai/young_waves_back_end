import { Post, Comment } from "../models/index.js";
const LikeController = {
   // [GET] likes/
   async show(req, res) {},

   // [POST] likes/:id
   async edit(req, res) {},
};

export default LikeController;
